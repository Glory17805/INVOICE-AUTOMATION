# Everything in the runtime set, plus what the test suite needs.
#
#   python -m pip install -r requirements-dev.txt
#   python -m pytest tests -q

-r requirements.txt
-r requirements-ocr.txt

pytest>=8.0,<10.0
ruff>=0.6,<1.0
