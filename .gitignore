backend/data/
backend/.env
__pycache__/
*.pyc
.pytest_cache/

# Backups are copies of data/, which is itself not tracked.
backend/backups/

# Local docker-compose settings (machine-specific paths).
/.env
